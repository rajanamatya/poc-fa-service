# Local development database (matches docker-compose.yml defaults)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=todos

# Production: leave the above unset and provide DB_SECRET_ARN.
# DB_SECRET_ARN=arn:aws:secretsmanager:us-east-1:000000000000:secret:rep/db-XXXXXX
